# Non-PE & Script YARA Rules

Place your multi-platform script rules (Python, Bash, AppleScript, Ruby, Office Macros, PDFs) in this directory.
When ready, enable `"non-pe"` in `config.yaml` under `active_platforms`.
